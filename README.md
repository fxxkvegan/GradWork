# GradWork

## Getting Started

install

```bash
composer install
```

env 作成（初回だけ）

```bash
cp .env.example .env
```

Key 生成（初回だけ）

```bash
php artisan key:generate
```

Dev

```bash
composer run dev
```
